eyetrax>=0.4.0
opencv-python>=4.8.0
numpy>=1.24.0
PyQt6>=6.4.0
pywin32>=306
scikit-learn>=1.3.0
mediapipe>=0.10.0

# ONNX Runtime con DirectML (GPU su Windows).
# Se non hai GPU o vuoi solo CPU, usa: onnxruntime>=1.16.0
onnxruntime-directml>=1.16.0

# Dipendenze runtime L2CS-Net (solo per tools/download_l2cs.py)
# torch>=2.0            # solo per conversione modello, non runtime
# torchvision>=0.15     # solo per conversione modello
# onnxmltools>=1.11     # solo per quantizzazione FP16
